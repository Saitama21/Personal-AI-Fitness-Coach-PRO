{
  "name": "forma-ai-mobile",
  "version": "0.2.0",
  "private": true,
  "description": "Mobile-first AI fitness coach PWA for iPhone, Railway-ready",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check server.js && node --check public/app.js && node --check public/workout-state.js && node --check src/plan-engine.js && node --check src/ai.js"
  }
}
